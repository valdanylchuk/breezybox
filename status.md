# Commands implementation status

## Present in some form:

+ cat
+ clear (send ANSI clear code)
+ cd
+ cp
+ date (very basic, e.g. fixed ISO format only)
+ df
+ du
+ echo
+ eget (install from github)
+ free (show heap memory, PSRAM) (add IRAM/SRAM separately)
+ head
+ httpd (foreground only)
+ ls
+ mkdir
+ more
+ mv
+ pwd
+ rm
+ sh (execute a .sh script)
+ tail
+ wc
+ wifi

## Next up:


## Later:

- fetch (sysinfo)
- grep
- gunzip
- gzip
- hexdump (canonic mode only)
- less
- null?
- ping
- ps (tasks?)
- sntp (basic time sync via wifi)
- tar
- telnet
- top
- unzip
- vi
- wget (HTTP download, esp_http_client)

## Much later:

- dmesg
- gpio
- lua (think how to integrate with the lib/component)
- mount
- nc
- reboot
- uptime
- which
